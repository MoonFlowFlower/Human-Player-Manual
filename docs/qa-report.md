# 首轮验证记录

日期：2026-09-14。对象：本次交付的 EARTH 本地实现。不是线上网站或现有 GitHub 仓库的验证报告。

## 实际运行

`npm run typecheck` 通过。`npm run lint` 通过严格类型与项目源码规则。`npm test` 的 **9 个测试全部通过**。`npm run build` 成功生成静态站与自包含 HTML，离线 HTML 约 128.4 KiB，gzip 约 39.0 KiB。尺寸不是 LCP 或实际网络加载速度测量。

测试涉及成本份额与传递比例、输入边界、任务顺序、损坏或未知版本的存储恢复、HTML 转义、特定问题意图排序、12 个概念的完整关系及来源 ID、笔记类别与长度边界。数据序列化通过不等于浏览器原生持久化通过。

## 浏览器交互检查

使用系统 Chromium 与 Playwright，桌面视口 1440×900、移动模拟视口 390×844。加载的是 `dist/EARTH-player-manual.html` 中实际构建的 HTML/CSS/JS，不是另外做的截图模板，也没有模拟替代应用逻辑。

已通过：主页入口；SVG 首页节点导航；图集选择、鼠标拖拽、缩放和复位；地图键盘平移与节点焦点；条目往返；Ctrl/Cmd-K 搜索和结果选择；问题配方；未知问题的诚实空态；Esc 关闭；有序任务锁；答错解释与重试；预测前隐藏结果；滑块改变模型；观察表单与本人报告确认；完成页；同一会话中的状态与草稿保留；重置时取消保留、确认清除；未知路由；移动端独立节点浏览器和靠前的阅读入口；导航及选择后的焦点恢复；减少动态效果模式。运行过程中没有捕获到 JavaScript pageerror。

另检查 320×700、768×1024、1440×900、1920×1080 四种尺寸下的首页、图集、运输说明和首个任务，16 组页面/视口没有检测到页面水平溢出。这不是实际手机或全浏览器兼容性测试。

## 两轮界面精修

第一轮实际截图检查后，修正搜索问题意图被宽泛词抢占、SVG 链接跳到未知页、已开启任务残留锁图标等交互问题。将价格问题从伪线性箭头改为并列理解上下文；补上观察草稿保留、移动节点选择后的焦点恢复。压缩移动任务进度区，并把“阅读这个系统”提前到焦点节点附近。

第二轮依据重新渲染的截图，增加正文与操作标签可读性、改善非相关节点的视觉层级、扩大移动搜索关闭按钮和系统选择框的触控区域。重新运行交互测试并检查桌面路径和移动任务截图。没有把风格目标表述为获得 Awwwards 或完成专业评审。

## 明确未验证

环境中的 Chromium 管理策略禁止所有 URL 导航，正常 HTTP 与 file:// 打开都会被阻止。没有修改或绕过该策略。浏览器测试使用 Playwright `set_content` 注入本地生产 HTML；因此不能声称测试了真实地址访问、部署路由、浏览器原生刷新、重启后的 localStorage 保留或浏览器历史按钮。受限存储的会话退化被实际执行；存储编解码与输入校验通过单元测试。

未完成 npm 注册表全新安装测试；使用的是交付环境中现成的 TypeScript 5.8.3。未做 Lighthouse、专业屏幕阅读器审计、实际 iOS/Android、Safari/Firefox、独立代码审计、专业内容审校或真人学习效果测试。两道教学判断通过不能证明用户掌握所有知识；现场观察是自动化测试填写的样例，不是实际做过的物品调查。

目标 GitHub 仓库未通过当前连接找到，所以没有 push、PR、上线或修改其他仓库。只有本地 Git 检查点。

## 复现与最小剩余验收

Node 测试运行 README 中四条命令。浏览器脚本为 `qa/browser_check.py` 与 `qa/interaction_regressions.py`，需要 Python Playwright 和 Chromium，可用 `CHROMIUM_PATH` 指定浏览器路径；未指定时优先系统 Chromium，随后使用 Playwright 已安装的浏览器。脚本不会修改浏览器策略。

在目标电脑上，最小剩余验收是：通过本机 HTTP 打开构建 → 完成两个教学步骤并填写观察草稿 → 刷新与关闭重开 → 核对状态及草稿 → 用浏览器前进/后退检查路由。之后再让一个没有看过项目说明的人完成路线，观察他是否知道下一步做什么、能否解释至少一条关系。不要在这些结果出来之前扩张条目数量。
